// Noda использует один предсказуемый графитовый интерфейс на телефоне и в web.
// Это также исключает светлый первый кадр во время гидратации статического Expo Router.
export function useColorScheme(): 'dark' {
  return 'dark';
}
